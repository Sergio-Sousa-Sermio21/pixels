Script A:
    Compiles files from src and runs all tests in the "../src/build/tests/." folder if any exist, then archives them.

Script B:
    Creates new version in "archive" and copies contents of folder "src" to it.
